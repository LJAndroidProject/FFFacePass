<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:typeface">serif</item>

        <item name="android:windowFullscreen">true</item>
    </style>


    <!--控制台按钮样式-->
    <style name="controlActivityButtonStyle" >
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_height">180dp</item>
        <item name="android:layout_width">156dp</item>
    </style>


    <!--圆形涟漪的点击效果-->
    <style name="RippleWhite">
        <item name="android:colorControlHighlight">#F6F6F6</item>
        <item name="android:radius">25dp</item>
    </style>


    <!--给toolbar单独设置的theme，让toolbar上按钮颜和文字颜色变化的 https://www.jianshu.com/p/81d0bcb282cb  -->
    <style name="MyDarkToolBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--toolbar图标颜色-->
        <item name="colorControlNormal">#FFFFFF</item>
        <!--toolbar的title颜色-->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <!--toolbar的subtitle颜色-->
        <item name="subtitleTextColor">#FFFFFF</item>
    </style>

    <!--给toolbar的menu内单独设置的theme，让toolbar上按钮颜和文字颜色变化的-->
    <style name="MyLightPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <!--设置背景-->
        <item name="android:background">@android:color/white</item>
        <!--设置字体颜色-->
        <item name="android:textColor">@color/colorAccent</item>
        <!--设置不覆盖锚点-->
        <item name="overlapAnchor">false</item>
    </style>

    <!--主题按钮-->
    <style name="themeButtonStyle">
        <item name="android:background">@drawable/control_btn_shape</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

</resources>
